# Local style reference

This directory is reserved for `sr_en_annual-report_pages_kaspersky_2025.pdf`.

The client PDF is deliberately excluded from the public repository. It remains a local visual reference and is not a content source or a template for literal copying.
